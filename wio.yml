type: app

project:
  name: alive-heartbeat-test
  version: 0.0.1
  keywords:
  - wio
  - app
  compile_options:
    wio_version: 0.5.1
    default_target: main

targets:
  main:
    src: src
    platform: avr
    framework: cosa
    board: uno

dependencies:
  can-packet:
    version: 1.0.0
  mcp2515-cosa:
    version: 1.0.0
  mcp2515-driver:
    version: 1.0.0

